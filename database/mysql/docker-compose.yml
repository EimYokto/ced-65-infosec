version: '3.1'

services:

  mysql:
    image: mysql:latest # use the latest image.
    container_name: mysql
    restart: always
    environment:
      POSTGRES_PASSWORD: mypassmysql
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080